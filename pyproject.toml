[project]
name = "hack-uk-ai-pdf-tagger"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "\"Akshay Akula\"", email = "akulaakshay30@gmail.com" }
]
dependencies = [
    "streamlit>=1.39.0",
    "pandas>=2.2.3",
    "numpy>=2.1.1",
    "pypdf2>=3.0.1",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/hack_uk_ai_pdf_tagger"]
